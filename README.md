# TastyShop 🍕

Современный интернет-магазин пиццы, построенный на Next.js 16 с использованием TypeScript, React 19 и Prisma ORM.

## 📋 Описание проекта

TastyShop — это полнофункциональное веб-приложение для заказа пиццы с следующими возможностями:

- **Каталог товаров** с фильтрацией и поиском
- **Корзина покупок** с возможностью выбора размера, типа пиццы и дополнительных ингредиентов
- **Система аутентификации** через email/password и GitHub OAuth
- **Верификация email** через отправку кода подтверждения
- **Оформление заказов** с автоподсказками адресов (DaData)
- **Истории (Stories)** для промо-материалов
- **Адаптивный дизайн** для всех устройств

## 🛠 Технологический стек

- **Frontend Framework**: Next.js 16.1.1 (App Router)
- **UI Library**: React 19.2.3
- **Language**: TypeScript 5
- **Styling**: Tailwind CSS 4
- **UI Components**: Radix UI, Shadcn/ui
- **State Management**: Zustand
- **Form Handling**: React Hook Form + Zod
- **Database**: PostgreSQL + Prisma ORM
- **Authentication**: NextAuth.js
- **Email Service**: Resend
- **Address Suggestions**: DaData API
- **HTTP Client**: Axios

## 📦 Установка и запуск

### Предварительные требования

- Node.js 20+ 
- pnpm (рекомендуется) или npm/yarn
- PostgreSQL база данных
- Аккаунты для сервисов (см. раздел "Переменные окружения")

### Шаги установки

1. **Клонируйте репозиторий** (если еще не сделано):
```bash
git clone <repository-url>
cd TastyShop
```

2. **Установите зависимости**:
```bash
pnpm install
```

3. **Настройте переменные окружения**:
Создайте файл `.env.local` в корне проекта и заполните его согласно разделу "Переменные окружения" ниже.

4. **Настройте базу данных**:
```bash
# Сгенерируйте Prisma Client
pnpm prisma:generate

# Примените миграции к базе данных
pnpm prisma:push

# (Опционально) Заполните базу тестовыми данными
pnpm prisma:seed
```

5. **Запустите проект в режиме разработки**:
```bash
pnpm dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

### Другие полезные команды

```bash
# Сборка для production
pnpm build

# Запуск production сборки
pnpm start

# Открыть Prisma Studio (GUI для базы данных)
pnpm prisma:studio

# Проверка кода линтером
pnpm lint
```

## 🔐 Переменные окружения

Создайте файл `.env.local` в корне проекта со следующими переменными:

### Обязательные переменные

#### 1. База данных PostgreSQL

```env
DATABASE_URL="postgresql://username:password@localhost:5432/tastyshop?schema=public"
```

**Настройка через Prisma и Vercel:**

##### Вариант 1: Локальная разработка

1. **Установите PostgreSQL локально:**
   - macOS: `brew install postgresql@14` или используйте [Postgres.app](https://postgresapp.com/)
   - Windows: скачайте с [официального сайта](https://www.postgresql.org/download/windows/)
   - Linux: `sudo apt-get install postgresql` (Ubuntu/Debian)

2. **Создайте базу данных:**
   ```bash
   # Подключитесь к PostgreSQL
   psql postgres
   
   # Создайте базу данных
   CREATE DATABASE tastyshop;
   
   # Выйдите из psql
   \q
   ```

3. **Настройте DATABASE_URL в `.env.local`:**
   ```env
   DATABASE_URL="postgresql://postgres:your_password@localhost:5432/tastyshop?schema=public"
   ```
   Замените `postgres` и `your_password` на ваши учетные данные PostgreSQL.

4. **Примените схему Prisma к базе данных:**
   ```bash
   # Сгенерируйте Prisma Client
   pnpm prisma:generate
   
   # Примените схему к базе данных (создаст все таблицы)
   pnpm prisma:push
   
   # (Опционально) Заполните базу тестовыми данными
   pnpm prisma:seed
   ```

##### Вариант 2: Vercel Postgres (для production)

1. **Создайте проект на Vercel:**
   - Перейдите на [Vercel Dashboard](https://vercel.com/dashboard)
   - Создайте новый проект или откройте существующий

2. **Добавьте базу данных Vercel Postgres:**
   - В настройках проекта перейдите в раздел **Storage**
   - Нажмите **Create Database** → выберите **Postgres**
   - Выберите регион (рекомендуется ближайший к вашей аудитории)
   - Нажмите **Create**

3. **Получите connection string:**
   - После создания базы данных Vercel автоматически создаст переменные окружения:
     - `POSTGRES_URL` - стандартный connection string
     - `POSTGRES_PRISMA_URL` - оптимизированный для Prisma (с connection pooling)
   - В настройках проекта Vercel добавьте переменную `DATABASE_URL` и установите её значение равным `POSTGRES_PRISMA_URL`
   - Или используйте напрямую `POSTGRES_PRISMA_URL` в коде (если Prisma настроен на её использование)

4. **Примените миграции на Vercel:**
   - Vercel автоматически запустит `prisma generate` во время сборки
   - Для применения схемы добавьте в настройках проекта **Build Command**:
     ```bash
     pnpm prisma generate && pnpm prisma db push && pnpm build
     ```
   - Или используйте Vercel CLI локально:
     ```bash
     vercel env pull .env.local
     pnpm prisma:push
     ```

5. **Альтернатива: Используйте Prisma Migrate (рекомендуется для production):**
   ```bash
   # Создайте миграцию
   pnpm prisma migrate dev --name init
   
   # Примените миграции на production
   pnpm prisma migrate deploy
   ```

**Примечание:** В проекте используется `prisma db push` для быстрой разработки. Для production рекомендуется использовать `prisma migrate` для версионирования миграций.

#### 2. NextAuth Secret

```env
NEXTAUTH_SECRET="your-secret-key-here"
```

**Как получить:**
- Сгенерируйте случайную строку (минимум 32 символа)
- Можно использовать команду: `openssl rand -base64 32`
- Или онлайн-генератор: [generate-secret.vercel.app](https://generate-secret.vercel.app/32)

#### 3. GitHub OAuth (опционально, но рекомендуется)

```env
GITHUB_ID="your-github-client-id"
GITHUB_SECRET="your-github-client-secret"
```

**Как получить:**
1. Перейдите на [GitHub Developer Settings](https://github.com/settings/developers)
2. Нажмите "New OAuth App"
3. Заполните форму:
   - **Application name**: TastyShop (или любое другое имя)
   - **Homepage URL**: `http://localhost:3000` (для разработки) или ваш production URL
   - **Authorization callback URL**: 
     - Для разработки: `http://localhost:3000/api/auth/callback/github`
     - Для production: `https://your-domain.com/api/auth/callback/github`
4. После создания приложения скопируйте **Client ID** и **Client Secret**
5. Вставьте их в `.env.local` (для разработки) или в настройки Vercel (для production)

**Важно для production:** Обновите Authorization callback URL в настройках GitHub OAuth App на ваш production домен перед деплоем.

#### 4. Resend API Key (для отправки email)

```env
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxx"
```

**Как получить:**
1. Зарегистрируйтесь на [Resend](https://resend.com)
2. Перейдите в [Dashboard](https://resend.com/api-keys)
3. Нажмите "Create API Key"
4. Дайте ключу имя (например, "TastyShop Development")
5. Скопируйте созданный API ключ (начинается с `re_`)
6. **Важно**: После создания ключ показывается только один раз, сохраните его!

**Примечание**: 
- По умолчанию Resend отправляет email с адреса `onboarding@resend.dev`. Для production рекомендуется настроить собственный домен в настройках Resend.
- Без этого ключа отправка email (верификация, подтверждение заказа) не будет работать.

#### 5. DaData API Token (для подсказок адресов)

```env
NEXT_PUBLIC_DADATA_API_KEY="your-dadata-api-key"
```

**Как получить:**
1. Зарегистрируйтесь на [DaData](https://dadata.ru)
2. Перейдите в [Личный кабинет](https://dadata.ru/profile/#info)
3. В разделе "API" найдите ваш API ключ
4. Скопируйте ключ в `.env.local`

**Примечание**: 
- Без этого ключа автоподсказки адресов не будут работать (приложение запустится, но поле адреса будет работать как обычный input без подсказок).
- Ключ используется в компоненте `AddressInput` для интеграции с DaData API.
- Это переменная с префиксом `NEXT_PUBLIC_`, поэтому она доступна в клиентском коде.

#### 6. API URL (опционально)

```env
NEXT_PUBLIC_API_URL="http://localhost:3000/api"
```

**Примечание**: 
- Если все API роуты находятся в `/app/api` на том же домене, можно не указывать эту переменную (axios будет использовать относительные пути).
- Указывайте эту переменную, если API находится на другом домене или порту.
- Это переменная с префиксом `NEXT_PUBLIC_`, поэтому она доступна в клиентском коде.

### Пример полного `.env.local`

```env
# Database
# Для локальной разработки используйте DATABASE_URL с вашим connection string
# Для Vercel Postgres: в настройках проекта Vercel установите DATABASE_URL = POSTGRES_PRISMA_URL
DATABASE_URL="postgresql://postgres:password@localhost:5432/tastyshop?schema=public"

# NextAuth
NEXTAUTH_SECRET="your-generated-secret-key-min-32-chars"

# GitHub OAuth
GITHUB_ID="your-github-client-id"
GITHUB_SECRET="your-github-client-secret"

# Resend Email
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxx"

# DaData API
NEXT_PUBLIC_DADATA_API_KEY="your-dadata-api-key"

# API URL (опционально)
NEXT_PUBLIC_API_URL="http://localhost:3000/api"
```

## 📁 Структура проекта

```
TastyShop/
├── app/                    # Next.js App Router
│   ├── (checkout)/        # Группа роутов для оформления заказа
│   ├── (root)/            # Основные роуты приложения
│   │   ├── @modal/        # Parallel routes для модальных окон
│   │   ├── product/       # Страница товара
│   │   ├── profile/       # Профиль пользователя
│   │   └── layout.tsx     # Layout для основных роутов
│   ├── api/               # API endpoints
│   │   ├── auth/          # NextAuth роуты
│   │   ├── cart/          # API корзины
│   │   ├── products/      # API товаров
│   │   └── ...
│   ├── layout.tsx         # Корневой layout
│   └── Providers.tsx      # React провайдеры
├── components/            # Переиспользуемые компоненты (legacy)
├── entities/             # Бизнес-сущности (Feature-Sliced Design)
│   ├── cart/             # Корзина
│   ├── category/          # Категории
│   └── product/           # Товары
├── features/              # Функциональные возможности
│   ├── auth/              # Аутентификация
│   ├── checkout/          # Оформление заказа
│   └── product-selection/ # Выбор товара
├── shared/                # Общие модули
│   ├── constants/         # Константы
│   ├── hooks/             # React хуки
│   ├── lib/               # Утилиты
│   ├── services/          # API сервисы
│   ├── store/             # Zustand store
│   ├── types/             # TypeScript типы
│   └── ui/                # UI компоненты
├── widgets/               # Виджеты (Feature-Sliced Design)
│   ├── container/         # Контейнер
│   ├── filters/           # Фильтры
│   ├── header/            # Шапка сайта
│   ├── products-list/     # Список товаров
│   ├── search/            # Поиск
│   └── stories/           # Истории
├── prisma/                # Prisma схема и утилиты
│   ├── schema.prisma      # Схема базы данных
│   └── seed.ts            # Сидер для тестовых данных
└── public/                # Статические файлы
```

## 🎯 Основные возможности

### Аутентификация
- Регистрация через email/password
- Вход через GitHub OAuth
- Верификация email через код подтверждения
- Защищенные роуты

### Каталог товаров
- Поиск по названию
- Фильтрация по категориям
- Фильтрация по цене (range slider)
- Адаптивная сетка товаров

### Корзина
- Добавление товаров в корзину
- Выбор размера пиццы
- Выбор типа теста
- Добавление дополнительных ингредиентов
- Изменение количества
- Подсчет общей стоимости

### Оформление заказа
- Форма с валидацией
- Автоподсказки адресов (DaData)
- Сохранение заказа в базу данных
- Отправка email подтверждения

### Профиль пользователя
- Просмотр истории заказов
- Редактирование данных

## 🚀 Деплой

### Vercel (рекомендуется)

1. **Подключите репозиторий к Vercel:**
   - Перейдите на [Vercel Dashboard](https://vercel.com)
   - Нажмите "Add New Project"
   - Подключите ваш Git репозиторий (GitHub, GitLab, Bitbucket)

2. **Настройте переменные окружения:**
   - В настройках проекта перейдите в раздел **Environment Variables**
   - Добавьте все необходимые переменные:
     - `DATABASE_URL` (или используйте `POSTGRES_PRISMA_URL` из Vercel Postgres)
     - `NEXTAUTH_SECRET`
     - `GITHUB_ID` и `GITHUB_SECRET`
     - `RESEND_API_KEY`
     - `NEXT_PUBLIC_DADATA_API_KEY`
     - `NEXT_PUBLIC_API_URL` (если отличается от дефолтного)

3. **Настройте PostgreSQL базу данных:**
   - Создайте Vercel Postgres (см. раздел "База данных PostgreSQL" выше)
   - Убедитесь, что `DATABASE_URL` указывает на `POSTGRES_PRISMA_URL`

4. **Обновите GitHub OAuth App:**
   - В настройках GitHub OAuth App обновите **Authorization callback URL** на ваш production домен
   - Формат: `https://your-domain.vercel.app/api/auth/callback/github`

5. **Настройте Build Command (опционально):**
   - Если нужно применить миграции при деплое, добавьте в **Build Command**:
     ```bash
     pnpm prisma generate && pnpm prisma db push && pnpm build
     ```

6. **Запустите деплой:**
   - Нажмите "Deploy"
   - После успешного деплоя приложение будет доступно по адресу `https://your-project.vercel.app`

### Другие платформы

Проект можно задеплоить на любую платформу, поддерживающую Next.js:
- [Netlify](https://netlify.com)
- [Railway](https://railway.app)
- [Render](https://render.com)
- Любой VPS с Node.js

## 📝 Лицензия

Проект создан в образовательных целях.

**Примечание**: Убедитесь, что все переменные окружения правильно настроены перед запуском проекта. Без них приложение не будет работать корректно.
